name: Tests

on:
  push:
    tags:
      # Only match tags like v1.0 or v2.1, not patch versions like v1.0.1
      - "v[0-9]+.[0-9]"
      - "v[0-9]+.[0-9].[0]"
  pull_request:
    branches: [main]

jobs:
  test:
    # Skip for patch version changes
    if: ${{ !contains(github.ref, '.0.') || github.event_name == 'pull_request' }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: |
          pytest src/web_button_watcher/tests/ --cov=web_button_watcher --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          fail_ci_if_error: true
